mutation DeleteFile($options: DeleteFileMutationInput!) {
  DeleteFile(options: $options) {
    data
    errors {
      name
      details {
        field
        value
        message
      }
    }
  }
}
